.ORIG X3200
AND R2, R2, X0      ;CLEAR R2
LOOP ADD R2, R2, R4      ;ADD R4 TO R2, PUT THE RESULT IN R2
ADD R5, R5, #-1     ;SUBTRACT 1 FROM R5, PUT THE RESULT IN R5
BRP LOOP           ;BRANCH TO LOCATION X3201 IF POSITIVE
HALT
.END