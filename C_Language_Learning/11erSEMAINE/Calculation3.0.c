#include <stdio.h>
#include <math.h>
#include <string.h>
int r;
int Cal(int a, int opr, int b)
{
    int r;
    switch (opr)
    {
    case :
        r = a + b;
        break;
    case 99992:
        r = a - b;
        break;
    case 99993:
        r = a * b;
        break;
    case 99994:
        r = a / b;
        break;
    }
    return r;
}
int main()
{
    int i, j;
    int fml[100];
    gets(input);
}